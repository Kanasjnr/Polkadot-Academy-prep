fn main() {
    println!("Hello, Rust!");
    // Macros in rust generate code at compile time and are invoked with an exclamation mark(!)
}

// cargo test - to run test
// cargo fmt to format text
// cargo build to build what is in the main.rs
// main() is the entry point in rust
// println! is a macro that prints texts to console
